# returns a newly created string formed from the\
# original one by removing all leading whitespaces.

# Demonstrating the lstrip() method
print("[" + " tau ".lstrip() + "]")

print("www.cisco.com".lstrip("w."))

print("pythoninstitute.org".lstrip(".org"))
